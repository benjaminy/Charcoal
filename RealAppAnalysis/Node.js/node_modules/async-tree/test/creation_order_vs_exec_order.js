require("async-tree");
printBuf = [];

//1
setTimeout(() => 0, 100);

//2
setImmediate(() => 0);

function foo(calls){
	if(calls == 0){
		return;
	}

	else{
		setImmediate(foo, calls - 1)
	}
}

//3
setTimeout(() => foo(3), 50)

//Expected 2, 3, 1

